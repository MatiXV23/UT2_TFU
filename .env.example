# Puerto del host donde se publica el gateway (unico puerto expuesto).
# Si 8080 esta ocupado, scripts/start.sh elige automaticamente otro libre.
PUERTO_HOST=8080

# Clave de firma de los JWT. En un entorno real debe ser un secreto gestionado.
JWT_SECRET=clave-demo-tfu-ut2-cambiar-en-produccion
